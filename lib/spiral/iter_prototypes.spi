/// ## into_iterator
nominal into_iterator t = infer

/// ## iter_collect
prototype iter_collect t u : into_iterator u -> t u

/// ## iter_collect''
prototype iter_collect'' w v : forall (t : * -> *) (u : * -> *). into_iterator (t (u v)) -> w v
